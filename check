set -e

./r < in > cur_out
./r < input2 > cur_out2

if ! (diff -Z cur_out sample_out > /dev/null); then
	echo "Wrong answer"
	exit 1
fi

if ! (diff -Z cur_out2 output2 > /dev/null); then
	echo "Wrong answer 2"
	exit 1
fi

echo "Accepted"
